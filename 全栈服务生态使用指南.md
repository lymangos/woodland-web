# 🥭 Woodland Mango 全栈服务生态使用指南

> **版本**：v1.2 (CI/CD + Backup Integrated)
> **更新日期**：2025-11-27
> **服务器 IP**：`64.23.227.107` (Vultr)

---

## 1. 对外公共服务 (Public Services)

这是普通用户访问你作品的入口。

### 🌐 主站点 (Web Portal)
* **访问地址**：[https://woodland-mango.click](https://woodland-mango.click)
* **功能**：沉浸式 React 前端体验（The Woodland）。
* **技术栈**：Vite + React + Tailwind CSS。
* **部署架构**：Cloudflare DNS (灰云) -> 雷池 WAF (清洗) -> Nginx (后端 62646 端口)。
* **安全特性**：全站强制 HTTPS，Brotli 压缩加速，HTTP/2 支持。

---

## 2. 管理与运维控制台 (Admin Consoles)

这是只有管理员（你）需要访问的后台，均已配置 HTTPS 加密保护。

### 🛡️ 雷池 WAF 控制台 (Security Center)
* **访问地址**：[https://waf.woodland-mango.click:9443](https://waf.woodland-mango.click:9443)
* **用途**：
    * 查看实时 QPS 流量、拦截黑客攻击。
    * 配置黑白名单（封禁恶意 IP）。
    * 管理 SSL 证书（已配置通配符证书）。
* **安全机制**：强密码 + 两步验证 (MFA)。

### 📊 Umami 统计后台 (Analytics)
* **访问地址**：[https://stats.woodland-mango.click](https://stats.woodland-mango.click)
* **用途**：
    * 查看真实访客数量 (Real-time Visitors)。
    * 分析用户地理位置、设备类型、来源渠道。
* **数据源**：前端代码自动上报 -> Docker 容器处理。

### 🚨 UptimeRobot 监控 (External Monitor)
* **访问地址**：[https://uptimerobot.com/dashboard](https://uptimerobot.com/dashboard)
* **用途**：外部哨兵。如果服务器宕机或断网，立即发送邮件报警。

### ☁️ Cloudflare DNS (Network Layer)
* **访问地址**：[https://dash.cloudflare.com](https://dash.cloudflare.com)
* **用途**：域名解析管理。
* **紧急开关**：如遇大规模 DDoS 攻击，将 A 记录由“灰云”改为“小黄云”以开启 CDN 隐藏 IP。

---

## 3. 日常运维操作 (Cheatsheet)

### 🚀 更新网站代码 (CI/CD 自动化)
**注意**：你已部署 GitHub Actions，**不再需要**登录服务器手动构建。

1.  **本地开发**：在 VSCode 修改代码。
2.  **提交推送**：
    ```bash
    git add .
    git commit -m "更新内容说明"
    git push
    ```
3.  **自动发布**：GitHub 会自动打包并覆盖服务器上的 `/var/www/woodland`，全程约 1-2 分钟。

### 💾 数据备份 (Backup)
服务器已配置 GPG 加密异地备份。

* **自动策略**：每天凌晨 04:00 自动打包并上传至 Google Drive。
* **手动触发备份**：
    ```bash
    sudo /root/scripts/backup.sh
    ```
* **查看备份日志**：
    ```bash
    cat /var/log/woodland_backup.log
    ```
* **私钥位置**：本地 Windows 电脑 (`woodland_private.key`) —— **请务必保管好，丢了无法恢复备份。**

### ⚙️ 修改 Nginx 配置
仅当你需要修改后端服务器设置（如端口、缓存规则）时使用：

1.  **编辑配置**：
    ```bash
    sudo nano /etc/nginx/sites-available/woodland
    ```
2.  **重载生效**：
    ```bash
    sudo nginx -t
    sudo systemctl reload nginx
    ```

### 🐳 管理 Docker 服务
当你需要重启雷池或 Umami 时：

* **重启 Umami**：
    ```bash
    cd /data/umami && docker compose restart
    ```
* **重启雷池**：
    ```bash
    cd /data/safeline && docker compose restart
    ```

---

## 4. 端口映射全景图 (Port Map)

| 端口 | 服务 | 状态 | 说明 |
| :--- | :--- | :--- | :--- |
| **80** | **雷池 WAF** | ✅ 开放 | HTTP 入口 (自动跳 HTTPS) |
| **443** | **雷池 WAF** | ✅ 开放 | HTTPS 流量主入口 |
| **9443** | **雷池后台** | ✅ 开放 | WAF 管理面板 (专用域名访问) |
| **22** | **SSH** | ✅ 开放 | 服务器远程连接 |
| **3000** | Umami | 🔒 仅内网 | 统计服务 (由雷池转发，勿开防火墙) |
| **62645** | Nginx | 🔒 仅内网 | HTTP 后端 (Mango味端口) |
| **62646** | Nginx | 🔒 仅内网 | HTTPS 后端 (Mango味端口) |
| **5432** | Postgres | 🔒 仅内网 | Umami 数据库 |

---

## 5. 紧急故障排查 (Troubleshooting)

如果网站打不开了 (`502 Bad Gateway` 或 `Connection Refused`)，请按顺序检查：

1.  **第一步：看雷池**
    * 能打开 `9443` 后台吗？
    * **能打开**：说明服务器活着，Docker 活着，大概率是 Nginx 挂了。
    * **打不开**：说明服务器可能死机了，或者 Docker 挂了。

2.  **第二步：看 Nginx (如果是 502)**
    * SSH 连上去输入 `sudo systemctl status nginx`。
    * 如果是 `failed`，输入 `sudo nginx -t` 看报错并修复。

3.  **第三步：看 Docker**
    * 输入 `docker ps`。
    * 检查 `safeline-tengine` 和 `umami` 是否都在 `Up` 状态。

4.  **终极手段**
    * 去 Vultr 后台控制面板，点击 **Restart** 强制重启服务器。
    * 重启后所有服务（Docker, Nginx, 防火墙）都会自动恢复。